#include "main.h"

/**
* _isdigit - this function checks if a character is a digit
*
* @c: this is the character to check
*
* Return: Always 1 if successful, and 0 if not
*
* File_name: 1-isdigit.c
*
* Author: Esther Ann Uduma
*/

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
